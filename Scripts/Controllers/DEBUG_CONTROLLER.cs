using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class DEBUG_CONTROLLER : MonoBehaviour
{
    /*
    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.Space) && FindObjectOfType<GameController>().hippoHerd.Count < 24)
        {
            GetComponent<HippoCreator>().CreateHippo(new Vector3(Random.Range(-8f, 8f), Random.Range(-4f, 4f), 0f));
        }

        if (Input.GetKeyDown(KeyCode.O) && FindObjectOfType<GameController>().currentYear < 10)
        {
            FindObjectOfType<GameController>().AdvanceGeneration();
        }

        if (Input.GetKeyDown(KeyCode.P))
        {
            SceneManager.LoadScene(0);
        }
    }
    */
}
